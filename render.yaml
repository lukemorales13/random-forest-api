services:
  - type: web
    name: random-forest-api
    env: python
    plan: free
    buildCommand: "pip install -r app/requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: MODEL_PATH
        value: model/srf_propio_model.pkl
